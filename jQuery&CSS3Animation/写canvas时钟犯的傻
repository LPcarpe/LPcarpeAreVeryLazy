1.没有beginPath导致的匪夷所思

2.错别字
    a).getContext写成getContent
    b).moveTo写成MoveTo，lineTo写成LineTo


3.数学方法非常天真地直接使用不加前缀Math

4.变量进行减法运算时不注意运算优先级不用括号如var x = Math.cos(rad) * r-20*rem;

5.把分号 ; 写成冒号 : 。

暂时发现脑子里只有这些水和草。

如果一开始的北京环境没有save和restroe就会改变原点从而导致匪夷所思的问题。

时分秒针弧度相加的算法注意一下。
